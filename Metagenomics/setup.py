from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The metagenomic pipeline section of the new atlantis github',
    author='New Atlantis',
    license='',
)
